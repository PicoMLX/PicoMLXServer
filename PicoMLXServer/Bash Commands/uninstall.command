# Script to uninstall all frameworks PicoMLXServer installs
# Use to restore computer to a state where only pip is installed
# For debugging purposes only.
# Do not add this script to any target.
#!/bin/sh

brew uninstall miniforge
